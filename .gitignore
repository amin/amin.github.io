node_modules/
public/
.superpowers/

# Per-machine Claude Code settings. .claude/ itself is tracked, so shared
# skills and settings.json are versioned — only the local overrides are not.
.claude/settings.local.json
