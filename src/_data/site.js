export default {
  // Absolute origin, used to build the absolute URLs Open Graph requires.
  url: "https://amin.sh",
  // Feeds both <title> and og:title — see the note in base.njk.
  ogTitle: "Amin El-Rifai — Full-stack developer in Gothenburg",
  // Mirrors the hero paragraph in home.njk, impersonally — this is the search
  // snippet and the share-card text, not a line someone reads on the page. The
  // two drifted once already when the hero was rewritten and this was not.
  description:
    "A full-stack developer building dependable web products, from thoughtful interfaces to the APIs that power them.",
};
